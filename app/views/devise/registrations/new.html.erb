<nav class="navbar2">
    <div class="navbar-header">
      <a class="navbar-brand" href="/" style="padding:0;margin-left: 30px;"><img src="../assets/dormus_logo-word-only1.png" style="width:200px;"/></a>
    </div/>
  <div class="container-fluid">
    <ul class="nav navbar-nav mynav">
       <li><a href="#">SUBMIT</a></li>
       <% if user_signed_in? %>
       <li><%= link_to "LOG OUT", destroy_user_session_path, method: :delete  %></li>
       <%else%>
       <li>
        <%= link_to('LOG IN', new_user_session_path)  %>  
        </li>
       <%end%>
    </ul>
  </div>
  </nav>
<body id="b-su">
	<section id="sign_up">
	 <div class="sign_up_form">
	   <div class="card log_form">
		   <center>
			<h2>Sign up</h2>

			<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
			  <%= f.error_notification %>

			  <div class="form-inputs">
			    <%= f.input :email, required: true %>
			    <%= f.input :password, required: true, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
			    <%= f.input :password_confirmation, required: true %>
			  </div>

			  <div class="form-actions">
			    <%= f.button :submit, "Sign up", :class=>"btn contribute btncont" %>
			  </div>
			<% end %><br>
			Have an account already? <%= render "devise/shared/links" %>
			</center>
		</div>
	  </div>
	</section>
<footer class="site-footer1">
	<a style="color: #fff;">About Us</a> | <a style="color: #fff;">Terms and Conditions</a> | Copyright 2015
</footer>
</body>

